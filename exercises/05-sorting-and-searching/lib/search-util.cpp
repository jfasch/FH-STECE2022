#include "search-util.h"

#include <algorithm>


Person find_person_by_name(std::vector<Person> persons, std::string name)
{
    // hier noch brav sein

    return Person(); // default initialized means not-found
}
