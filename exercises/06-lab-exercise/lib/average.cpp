#include "average.h"


double average(std::vector<std::pair<unsigned long, double>> measurements)
{
    // hier noch brav sein

    return 0;
}

std::map<std::string, double> averages(
    std::vector<std::pair<std::string, std::pair<unsigned long, double>>> measurements)
{
    std::map<std::string, double> rv;

    // hier noch brav sein

    return rv;
}
